# `app.py` 解説

`app.py` はこのアプリの「ホーム画面」を作っているファイル。上から順に説明する。

## 1〜4行目: 準備

```python
import streamlit as st
from utils.ui import setup_sidebar
```

- `streamlit`(略して`st`)は画面を作るためのライブラリ。`st.○○`と書くと、それがそのまま画面のパーツ(タイトル、ボタン、テキストなど)になる。
- `setup_sidebar` は自分たちで作った関数で、`utils/ui.py`の中に定義されている。サイドバーの「APIキー入力欄」を表示するための共通部品。

## 6〜10行目: ページの基本設定

```python
st.set_page_config(
    page_title="AIライティングツール",
    page_icon="✍️",
    layout="wide",
)
```

- ブラウザのタブに表示されるタイトルとアイコン(✍️)を設定している。
- `layout="wide"` は画面を横幅いっぱいに使う設定。

## 12行目: サイドバー表示

```python
setup_sidebar()
```

- この1行を呼ぶだけで、サイドバーに「APIキー入力欄」と「モデル選択」が表示される。全ページ(この`app.py`と`pages/`配下の各ツール)で同じ関数を呼んでいるので、どのページでも同じサイドバーが出る仕組み。

## 14〜22行目: タイトルと説明文

```python
st.title("✍️ AIライティングツール")
st.caption("...")
st.markdown("""...""")
```

- `st.title`は大きな見出し、`st.caption`は小さな補足文、`st.markdown`は自由なテキスト(Markdown記法が使える)。上から順にそのまま画面に表示されるだけ。

## 24行目: 区切り線

```python
st.divider()
```

- 画面に横線を引くだけの装飾。

## 26〜42行目: ツール一覧のカード表示

```python
tools = [
    ("📝", "ブログ記事作成", "..."),
    ("📧", "メール返信作成", "..."),
    ...
]

cols = st.columns(3)
for i, (icon, name, desc) in enumerate(tools):
    with cols[i % 3]:
        st.markdown(f"#### {icon} {name}")
        st.write(desc)
        st.markdown("")
```

ここが一番のポイント。

- `tools`は「(絵文字, ツール名, 説明文)」の組み合わせをリストにしたもの。
- `st.columns(3)` で画面を3列に分割する(`cols[0]`, `cols[1]`, `cols[2]`という3つの「置き場所」ができるイメージ)。
- `for i, (icon, name, desc) in enumerate(tools):` で `tools` を1個ずつ取り出しながら、`i`(0, 1, 2, 3...という連番)も一緒に取得する。
- `cols[i % 3]` が肝で、`%`は「割った余り」を出す演算。`i`が0,1,2,3,4,5,6,7と増えるにつれ、`i % 3`は0,1,2,0,1,2,0,1と繰り返される。つまり**8個のツールを3列に順番に並べていく**処理。
- `with cols[...]:` のブロック内に書いたものが、その列の中に表示される。ツール名(見出し)と説明文がカードのように並ぶ。

## 44〜48行目: 締めの注意書き

```python
st.divider()
st.caption("このアプリはデータベースや認証機能を持たない個人用ツールです。...")
```

- 最後にもう一度区切り線を引き、「DBも認証もない個人用ツールで、データはブラウザのセッション内だけに残る」という注意書きを表示している。

## まとめ

`app.py`は実際にAIを呼び出す処理は一切しておらず、以下の役割だけを持つ「入口ページ」。

1. サイドバーを出す
2. タイトルと説明を表示する
3. 8つのツールを紹介するカードを3列に並べる

実際の生成処理(Gemini APIを呼ぶ部分)は`pages/`配下の各ファイルにある。
